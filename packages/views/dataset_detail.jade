template(name="datasetDetail")
  .container
    .row
      .col-sm-12
        .dataset-detail
          +with dataset
            p Name: #{name}
            +with file
              if isUploaded
                a.btn.btn-primary(href="{{this.url download=true}}" target="_blank") Download
              else
                p Upload in progress