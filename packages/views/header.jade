template(name="header")
  header
    nav.navbar
      .container
        .navbar-header
          button.navbar-toggle.collapsed(type="button", data-toggle="collapse" data-target=".navbar-collapse") Menu
          a.navbar-brand(href="/")
            span Mantle
        .navbar-collapse.collapse
          ul.nav.navbar-nav.navbar-right
            if currentUser
              li
                a.navbar-btn(href="{{path route='newDataset'}}")
                  span + Add Dataset
            li
              a.navbar-btn(href="{{path route='organizations'}}")
                span Organizations
            +accountsHeaderButtons
