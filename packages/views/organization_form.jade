template(name="organizationForm")
  if currentUser
    .container
      h1 Add an Organization
      .row
        .col-sm-8
          form#new-organization-form
            .row
              .col-sm-8
                label Name
                  input#organization-name.form-control(name="name" type="text")
            .row
              .col-sm-12
                label Description
                  textarea#organization-description.form-control(name="description")
            input.btn.btn-primary(type="submit" value="Save")
  else
    .container
      .row
        .col-sm-12
          p Please log in or sign up to continue.
